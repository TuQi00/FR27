import React from 'react';

// MyButton.js
class MyButton extends React.Component {
    render() {
        // 1. Dùng cấu trúc rẽ nhánh để xác định a.
				// Code của bạn ...

				// 2. Gán b bằng với prop onClick.
				// Code của bạn ...

				// 3. Gán c bằng với prop children
				// Code của bạn ...

        return (
            <button className={a} onClick={b}>{c}</button>
        );
    }
}